[project]
name = "automation-simulator"
version = "0.1.0"
description = "UI end-to-end testing with Playwright and Pytest"
authors = [
    {name = "Sheikh",email = "kenneth.sheikh@hotmail.com"}
]
license = {text = "MIT license"}
readme = "README.md"
requires-python = "^3.11"
dependencies = [
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.group.dev.dependencies]
pytest = "^8.4.1"
playwright = "^1.54.0"

